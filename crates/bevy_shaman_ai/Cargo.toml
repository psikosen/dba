[package]
name = "bevy_shaman_ai"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bevy.workspace = true
bevy_shaman_core = { path = "../bevy_shaman_core" }
bevy_shaman_monsters = { path = "../bevy_shaman_monsters" }
bevy_shaman_story = { path = "../bevy_shaman_story" }
bevy_shaman_combat = { path = "../bevy_shaman_combat" }
serde.workspace = true
serde_json = "1.0"

# LLM inference - will use llama.cpp bindings
# Note: User will add the GGUF model file later
# For now, we'll create the infrastructure
