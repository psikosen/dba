use bevy::prelude::*;
use crate::components::Inventory;

/// Basic inventory management (placeholder for UI integration)
pub fn manage_inventory(
    inventories: Query<&Inventory>,
) {
    // Placeholder: integrate with UI system for display
    // This would handle sorting, filtering, etc.
}
